name: haewon_defense
description: "해원의 문 (Gateway of Regrets) - Korean Folklore Tower Defense RPG"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.35.1
  flame_riverpod: ^5.0.9
  flutter_riverpod: ^2.6.1
  flame_audio: ^2.10.6
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/data/
